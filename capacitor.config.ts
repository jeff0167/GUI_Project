import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MentalHealthSupport_APP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
